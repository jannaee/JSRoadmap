class Tree {
    constructor(add, remove, val){//a special method for create and initialize the tree object
       console.log(this.add = add);
        this.remove = remove;
        this.val = val;
    }

    Add () {//adding a node to the tree

    }

    Remove () {//remove node from the tree

    }
    
}//end of Tree
//new keyword is to create an object

class Bubble {
    constructor(){
//do not have to declare variable with normal keywords
//use this to  
this.y=100;
this.x=200
    }
}

let bubble = new Bubble();
